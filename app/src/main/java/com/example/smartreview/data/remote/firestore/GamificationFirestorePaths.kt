package com.example.smartreview.data.remote.firestore

/**
 * Firestore paths for gamification ledger under users/{uid}.
 */
object GamificationFirestorePaths {
    const val REWARD_LEDGER = "rewardLedger"
}
